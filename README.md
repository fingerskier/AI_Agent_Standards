# AI_File_Standards
A collection of standard documentation files for coding agents

## External Settings
* I use `npx xlii` for saving things that fall outside of repos
  * e.g. `CLAUDE.md` that lives in a head directory: this utility will save and restore those for general settings
